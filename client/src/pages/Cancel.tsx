import Container from "../ui/Container";
import Title from "../ui/Title";

const Cancel = () => {
  return (
    <Container>
      <Title title="Cancel Page" />
    </Container>
  );
};

export default Cancel;
